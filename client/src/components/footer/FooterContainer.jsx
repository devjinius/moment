import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  padding: 1rem;
  margin-top: 1rem;
`;

const FooterContainer = props => <Wrapper />;

export default FooterContainer;
